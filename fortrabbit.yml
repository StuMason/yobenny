version: 2

# called before Composer runs
pre: deployment/before.sh

# optional Composer settings
composer:
    prefer-source: true
    no-dev: true
    no-plugins: true
    no-scripts: true

# called after Composer runs
post: deployment/after.sh